# Core
numpy==1.26.4
pandas==2.2.2
pyyaml==6.0.1

# Embeddings (CPU). Model weights are cached locally so ranking runs offline.
sentence-transformers==2.7.0
torch==2.2.2            # CPU build
huggingface-hub==0.23.0

# Similarity / ANN (CPU only)
faiss-cpu==1.8.0

# Graph
networkx==3.3

# Fast IO (optional but used)
pyarrow==16.1.0

# Demo / sandbox
streamlit==1.35.0
plotly==5.22.0
pyvis==0.3.2
